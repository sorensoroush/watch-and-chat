import React from 'react'
import { ActionCable } from 'react-actioncable-provider'

/*
const Cable = props => {
  return (
    <>
      {props.rooms.map(room => {
        <ActionCable key={room.id} channel={{channel: 'MessagesChannel', room: room.id }} onReceived={console.log('message receieved!')} />
      })}
    </>
  )
}

export default Cable
*/
